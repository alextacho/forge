# dev/CLAUDE.md
You are working on the forge tooling layer only.
Only edit files inside `dev/scripts/`, `dev/fixtures/`, `skills/`, `commands/`, and `snippets/`.
Never edit anything in `workspace/` or `dist/`.
If asked to do something that requires touching a target plugin's application layer, stop and ask.
